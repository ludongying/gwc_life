<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seven.gwc.modular.munition.dao.MunitionInMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.seven.gwc.modular.munition.entity.MunitionInEntity">
        <id column="id" property="id" />
        <result column="action_type" property="actionType" />
        <result column="detail_id" property="detailId" />
        <result column="remark" property="remark" />
        <result column="status" property="status" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, action_type, detail_id, content, remark, status, apply_person, in_out_person, in_out_time
    </sql>

    <select id="maxCode" resultType="java.lang.Integer">
        SELECT count(*) FROM ship_munition_inandout WHERE code like CONCAT('%',#{dateCode},'%')
    </select>

    <select id="selectMunitionInList" resultType="com.seven.gwc.modular.munition.entity.MunitionInEntity">
        SELECT i.* FROM ship_munition_inandout i
        LEFT JOIN sys_user u ON i.create_person = u.id
        LEFT JOIN sys_dept d ON u.dept_id = d.id
        where i.delete_flag = 1
        <if test="munitionIn.code != null and munitionIn.code != ''">
            and i.code like CONCAT('%',#{munitionIn.code},'%')
        </if>
        <!-- 数据范围过滤 -->
        ${munitionIn.params.dataScope}
        order by i.create_date desc
        LIMIT #{total} , #{size}
    </select>

    <select id="getListSize" resultType="com.seven.gwc.modular.munition.entity.MunitionInEntity">
        SELECT i.* FROM ship_munition_inandout i
        LEFT JOIN sys_user u ON i.create_person = u.id
        LEFT JOIN sys_dept d ON u.dept_id = d.id
        where i.delete_flag = 1
        <if test="munitionIn.code != null and munitionIn.code != ''">
            and i.code like CONCAT('%',#{munitionIn.code},'%')
        </if>
        <!-- 数据范围过滤 -->
        ${munitionIn.params.dataScope}
    </select>


</mapper>
